# (Note): Some settings are stored as scores, while others (which rely on floating-point accuracy) are stored in settings.<type>.<name>
#         Values derived from settings (i.e., gravity acceleration per tick) are stored as scores (#Physics.Settings.Derived.<name>) or in data storages (settings.<type>.derived.<name>)
function physics:zprivate/settings/show_2 with storage physics:zprivate settings
